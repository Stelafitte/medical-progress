/**
 * Ancien magasin local des connaissances créées pendant la session de
 * maquette. Retiré une fois le référentiel branché sur Supabase (voir
 * `dataAccess.outcomes.createOutcome`, port `OutcomeRepository`). Fichier
 * conservé vide pour ne pas casser d'éventuels imports résiduels.
 */
export {};
