/**
 * RETIRÉ le 26/08/2026 : les droits sont désormais accordés pour de vrai via
 * `dataAccess.administration.grantRoleAssignment` (RPC Supabase
 * `grant_role_assignment`), plus via ce store local en mémoire.
 * Fichier conservé vide (non supprimable depuis l'environnement d'édition
 * utilisé) : aucun import ne doit plus en dépendre.
 */
export {};
